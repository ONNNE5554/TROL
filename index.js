const Discord = require('discord'); //import client from discord
const keep_alive = require('./keep_alive.js')
const client = new Discord.client();

Client.login(process.env.TOKEN); //login bot using token
